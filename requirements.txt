# ============================================================================
# Requirements: RLVR Experiments (Colab Pro A100)
# Confirmed working: TRL 1.0.0 + Transformers 5.0.0 (April 2026)
# ============================================================================
# Install: pip install -r requirements.txt
# ============================================================================

# Core training
trl==1.0.0
transformers==5.0.0
accelerate>=1.0.0

# Reasoning tasks
reasoning-gym>=0.1.0

# Data / utilities
datasets>=3.0.0
torch>=2.4.0           # pre-installed on Colab A100; leave unversioned for flexibility

# Math verification (only needed if using GSM8K environment)
math-verify>=0.9.0

# Optional: faster tokenization
tokenizers>=0.20.0
